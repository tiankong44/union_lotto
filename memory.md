# 项目记忆

## 当前目标
- 新增完整孕期助手：记录孕期档案、胎动、宫缩、体重血压、产检、症状和待办，并提供历史、统计、个人趋势提示、提醒、导出和本地优先后台同步。

## 技术现状
- 后端：Spring Boot 2.7.4、Java 8、MyBatis-Plus、MySQL、Redis，服务上下文为 `/tabs`。
- 前端：目录为 `pregnancy-assistant/`，采用 Vite + Vue + TypeScript。
- 现有相关模块：`apex` 仅支持按天累加数量，不直接复用为孕期记录模型。
- 当前已知未提交改动：`src/main/java/com/tiankong44/tool/workday/generator/ScheduleGenerator.java`，执行时必须保留。

## 业务约束
- 第一版只有一个使用者，不建立账号、用户隔离、家庭共享或医生协作模型。
- 数据本地优先保存，联网后通过幂等同步队列上传；记录失败时保留本地状态并允许重试。
- 统计和分析只比较用户自己的历史基线，不输出医疗诊断；减少或明显变化时提示联系产科。
- 业务信息未知时使用“待补充”，不得把代码实现直接当成业务事实。

## 业务需求修改汇总

| 日期 | 模块 | 变更摘要 | 影响范围 |
| --- | --- | --- | --- |
| 2026-08-06 | 孕期助手 | 新增完整孕期助手范围，确定单用户、本地优先同步和个人趋势提示。 | 前端、后端、数据库、业务知识库 |
| 2026-08-06 | 孕期助手 | 根据使用范围确认移除用户隔离字段、路径参数和用户标识展示。 | 前端、后端、数据库、业务知识库 |
| 2026-08-06 | 胎动记录 | 确认记录胎动作为打开首页，迁移总览入口并采用清透樱粉紧凑工作台。 | 前端路由、导航、记录页、总览页、样式 |

## 待补充知识
- 医疗机构/地区采用的具体胎动与宫缩提醒规则：待补充。
- 单用户服务端数据备份、恢复和并发访问策略：待补充。
- 数据保存期限、隐私政策和删除策略：待补充。
- 真实业务负责人、验收人和上线环境：待补充。

## 实现与验证结论
- 2026-08-06：完成 `pregnancy-assistant/` Vite + Vue + TypeScript 前端，包含胎动、宫缩、历史、统计趋势、孕期档案、健康记录、产检待办、导出和本地 IndexedDB 同步队列。
- 2026-08-06：将前端目录从 `frontend/` 重命名为 `pregnancy-assistant/`，NPM 包名和页面品牌名保持不变。
- 2026-08-06：完成单用户孕期后端接口、实体、Mapper、Service、Controller 和 `sql/init_pregnancy_assistant.sql`；同步接口会把载荷落入对应业务表，并按客户端记录编号幂等。
- 2026-08-06：修复 `MybatisPlusConfig` 未扫描 `com.tiankong44.tool.pregnancy.mapper` 导致孕期服务启动时 Mapper 注入失败的问题，JDK8 编译和临时端口启动验证通过。
- 2026-08-06：修复胎动记录完成后 IndexedDB 无法克隆 Vue 响应式快照的问题，保存前将档案和记录集合展开为普通对象，前端类型检查与生产构建通过。
- 2026-08-06：将 `/` 调整为胎动记录工作台，原总览迁移至 `/overview`，保留 `/movement` 兼容跳转；完成清透樱粉主题、紧凑 `hero-band`、响应式导航和保存状态反馈优化。
- 2026-08-06：前端 `npm run build` 通过；后端使用 `E:\JAVA\jdk8` 执行 `mvn clean compile` 通过。
- 2026-08-06：Java 21 下旧 Lombok 与编译器内部 API 不兼容，后续升级依赖前继续使用 Java 8 构建。
- 2026-08-06：按约束未使用浏览器截图或视觉辅助，未执行真实数据库联调；SQL 已完成静态禁止项检查。

## 维护记录
- 2026-08-06：初始化项目记忆，记录孕期助手建设范围和已确认约束。
- 2026-08-06：记录单用户改造、构建验证结论和已知 Java 21 环境兼容性。
