# 云端 MySQL 存储改造实施计划

> 本计划在当前仓库内执行，不创建 worktree，不使用子 Agent，不进行浏览器视觉验证。

**目标：** 删除业务本地持久化，将孕期记录和新上传文件统一保存到云端 MySQL。

**架构：** 前端通过现有后端 CRUD 接口直接读写 MySQL；上传文件通过独立文件表保存二进制内容。Redis继续承担现有缓存和令牌职责。

**验证：** 执行前端类型检查、生产构建、Java 编译、SQL 静态检查和本地存储引用扫描；不迁移旧浏览器数据及历史磁盘文件。

